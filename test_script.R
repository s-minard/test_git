usethis::git_sitrep()
#package::function tells r to use the function directly from the specific package without having to load the entire package
#also useful when multiple packages have the same function and you need to specify